// export test skipped — depends on old KeyGenResult type
import { describe, it } from 'vitest';

describe('export', () => {
  it('placeholder', () => {
    // export utility updated separately
  });
});
